---
Дата создания: " 28-10-2025"
Источник: https://youtu.be/QIfgGycBGfg?si=xEquUxr7Tfa2LsWw
tags:
---
---
## Выжимка

### Объекты
все значения в питоне это объекты а переменные просто ссылка на них

у каждого объекта есть поля id тип, значение, размер , счетчик ссылок

Не изменяемые объекты имеют фиксированный размер

реальный размер объекта не обязан совпадать с размером значения этого объекта


Создание объекта происходит в 2 этапа:
1. Создание объекта
2. создание переменной которая ссылается на этот объект

если счетчик ссылок объекта=0 то он вычещается из памяти

Объекты делятся на изменяемые  и неизменяемые (строки числа и тп)

##### Неизменяемые объекты:
к ним относятся:
числа
строки
и тп
Работа с неизменяемым типом данных:
``` python
a = 30 # создаем объект с значением 30, создаем переменную a и ссылаемся на объект увеличивая его счетчик ссылок на 1

a = 31 # создаем объект с значением 31, меняем ссылку переменной a c объекта 1 на объект 31, Удаляем объект 30 из памяти так как его счетчик ссылок стал равен 0

```

В питоне есть заранее созданные часто используемые объекты c фиксированными значениями:
```python
a = 5
b = 1*5
# а и b ссылаются на один и тот-же объект так как это частоиспользуемое значение

x = 555
y = 111 * 5

# x и y ссылаются на разные объекты

# Таким образом достигиеется баланс между скоростью выполнения программы (быстрее создать объект чем найти из созданных) и прожорливастью (Созбани нескольких одиноковых объектов потребляет больше памяти чем если бы мы ссылались несколькеми переменными на один объект)
```

##### Изменяемые объекты:
 идентификаторы изменяемых объекты остаются неизменны. значениями изменяемых объекты являются ссылки на любые другие объекты

к ним относятся:
списки 
словари
множества
и тп

`Поверхностное копирование` - при создании копии списка мы создаем новый список но ссылаема на значения из исходного списка вместо того чтобы создать копию этих элементов 

`Глубокое копирование` -  создание полной копии списка и всех его элементов с ковами индентификаторами

`Глубокое копирование` безопаснее использовать если не все элементы списка являются Неизменяемыми объедками

### Числа
float следует стандарту IEEE 754
```python
a = 1-0.99
print(a) # 0.01000000000000009
```

эта проблема связана с приведением десятичного числа к двоичному. алгоритм конвертачии при подаче в него числа на подобии 0.99 может работать бесконечно если его не прервать. именно из-за прерывания и появляется погрешность

для работы с числами с повышенной точностью существует модуль `import decima`

### dir() и help()

dir() - принимает любой объект и возвращает список атрибутов и методов объекта

help() - принимает метод объекта и возвращает его описание. При вызове help() без параметров мы переходим в интерактивный режим


### None

None - означает пусто. у этого значение свой класс NoneType. При выполнении код питон всегда хранит только один объект None и просто ссылается на него

### Параметры по умолчанию

В качестве параметров по умолчанию желательно не использовать Изменяемые  объекты так как они инициализируются только один раз и могут запомнить изменения между выводами функций:
```python

def elements(a,lot=set()):
	lot.update(a)
	print(lot)
	print(id(lot)) # идентификатор объекта всегда одинаковый так как параметры по умолчанию создаются в момен определения а не вызова функции
	
elements("день") # выведит {"д","е","н","ь"}

elements("день") # выведит {"д","е","н","о","ч","ь"} 😮🫣
```

### Работа с файлами
---
TODO

### Пространства имен и область видимости
![[Пространства имен python.png]]

вывод словарей имен для каждой области:
```python
print(f"все встоенные переменные:{dir(__builtins__)}")
print(f"локальные переменные функции:{locals()}")
print(f"все глобальные переменные:{globals()}")
```

порядок поиска имен зависит от того где было вызвано триггер событие поиска

### Модули пакеты и библиотеки
TODO

## Транскрипция
основой для создания данного видео является книга Мэта харрисона Как устроен Python видео Рассчитано скорее на тех у кого уже есть начальное представление о языке Python Хотя в книге и поясняются базовые вещи я их упускаю во-первых так как они описаны почти в каждой книге для начинающих питани чтов во-вторых объём информации и так получился не маленьким В данном видео я постарался выделить информацию прежде всего связанную с внутреней ра Python то есть акцент на процессы скрытые от глаз в процессе создания видео у меня возникали Вопросы ответы на которые приходилось искать в других источниках поэтому это не просто пересказ книги А в какой-то мере и её дополнение Надеюсь что и вы найдёте в видео что-то для себя новое Приятного просмотра мир Python состоит из объектов исключением являются лишь ключевые слова для того чтобы можно было обращаться К объектам используются переменные по факту каждый объект наделяется уникальным не обязательно единственным именем автор проводит аналогию с фермой где у каждой коровы с целью их идентификации есть бирка с именем в мире Python объект - Это коровы А их бирки - это переменные при этом у одной коровы может быть несколько бирок как и несколько переменных могут ссылаться на один и тот же объект продолжая данную аналогию бирки мо можно перевесить с одной коровы на другую или даже не обязательно корову А например на барашка так и переменной можно присвоить новое значение при этом тип значения может измениться бирки помогают отслеживать животных так и переменные позволяют обращаться к нужным объектам из программного кода каждый объект в Python помимо уникальных свойств зависящих от типа объекта и реализации имеет общие свойства такие как идентификатор размер значение счётчик ссылок для примера рассмотрим объект в виде строки я объект идентификатор указывает на адрес в оперативной памяти в котором Python хранит этот объект вызвав функцию ID можно получить значение идентификатора в пределах времени жизни объекта его значение уникально то есть два разных объекта не могут иметь одновременно один и тот же идентификатор Но если в течение жизненного цикла программы объект был удалён его адрес в оперативной памяти может занять Новый объект под типом объекта подразумевается класс объекта существуют различные типы К тому же можно создать свои для вывода на экран информации о типе объекта используется функция Type в нашем случае объект имеет тип стрин строка размеры объектов указываются как целое число и измеряются в байтах изменяемые объекты имеют фиксированное значение в то время как изменяемые могут менять свой размер во время выполнения программы реально занимаемый объём памяти объекта может быть отличен от размера который занимают непосредственно данные объекта так как необходимо учитывать дополнительный объём памяти который уходит на хранение и другой информации об объекте Ну и у объекта имеется значение присваивание значения переменной происходит два этапа на первом этапе Python создаёт объект после этого Python создаёт переменную и связывает её с этим объектом если переменная была создана раньше и уже связана с другим объектом то предыдущая связь обрывается в пользу связи с новым объектом у каждого объекта имеется счётчик ссылок который хранит информацию о количестве переменных и других объектов ссылающихся на данный объект если переменная перестаёт ссылаться на объект значение у счётчика ссылок уменьшается когда значение уменьшается до нуля интерпретатор Python делает Вывод что объект не востребован и в работу включается уборщик мусора который удаляет невостребованный объект из памяти контролируя таким образом использования памяти для получения значения счётчика ссылок для объекта используется функция Get модуля Assis чтобы схемы взаимодействия объектов не выглядели перегружен в дальнейшем объекты будут обозначаться без счётчика ссылок и размера разберём ещё одно свойство объекта изменяемость объекты бывают изменяемые и неизменяемый К неизменяемым объектам относятся например строки числа кортежи Судя по названию данные объекты не позволяют себя изменить Что это значит разберём пример имеется Объект который отражает количество сотрудников к примеру их 30 значение данного объекта присвоено переменной с именем emplo данный объект размещен в памяти по определённому адресу мы его выведем на экран при этом так как переменная ссылается именно на этот объект значение переменной находится по тому же адресу нуно Так как речь идёт об одном и том же объекте тип объекта целое число значение 30 в штат взяли ещ одного сотрудника и логично что количество сотрудников изменилось с до 31 то есть значение переменной росло на единицу инструкция меняющая значение переменной Лоя состоит из двух шагов на Первом шаге значение переменно увеличивается на единицу на втором шаге это новое значение присваивается переменной emplo Что происходит в это время под капотом Python после того как поступила Команда А при вращении значения на единицу в оперативной памяти был создан Новый объект и идентификатор которого отличаются от уже существующего после чего производится присваивание старой переменной нового значения то есть переменная теряет связь со старым объектом И обретает связь с новым то что переменная теперь ссылается на Новый объект доказывает её новое значение идентификатора которое совпадает с идентификатором нового объекта из данного примера заключаем что если обе Натой ссе отно к немее типу то при изменении значения переменной переменная будет связываться с новым объектом что влечёт за собой и изменения идентификатора переменной мои наблюдения мне показалось логичным что если во время исполнения программы в памяти создаётся объект неизменяемой При появлении объекта с идентичным значением интерпретатор будет использовать объект уже ранее созданный я заблуждался поясню что имеется в виду К примеру присвоила значение 5 переменной Y присвоил в результате перемножения единицы и пятёрки Во время Первой инструкции в памяти был создан объект со значением 5п и естественно своим адресом каким-то значением идентификатора во время Второй операции переменная Y была присвоено значение равное п то есть обе переменные имеют одно и тоже значение равное если вывести их идентификаторы то они окажутся равными Это означает что переменная Y ссылается на уже ранее созданный в памяти объект на тот что ссылается переменная X теперь присвоить переменной X новое значение 555 а переменной Y присвоив значение равное произведению 111 и п Как видим значения X и Y равны выведем на экран идентификаторы переменных X и Y они неравны значит Несмотря на то что в памяти уже существует объект со значением 555 Python создаёт в памяти Новый объект со значением равным значению уже существующего в памяти объекта возникает два вопроса Почему при одних и тех же операциях мы получаем разные результаты Почему возникают случаи когда в памяти создаются неизменяемые объекты с равными значениями ведь Это увеличивает расход памяти ответ оказался простым проверка наличия объектов памяти отнимает ресурсы и не имеет смысла для редких или временных объектов Поэтому в качестве компромисса между скоростью выполнения программы и эффективностью управления памяти проверяются только небольшие и часто используемые значения например небольшие числа для других значений иногда Проще создать новый объект чем осуществлять поиск среди существующих объектов перейдём к изменяемым об изменяемые объекты допускают изменения своего значения прямо по адресу их нахождения то есть при изменении их значений их идентификатор остаётся неизменным к изменяемым типам относятся например списки словари множество перечисленные объекты состоят из ссылок на другие объекты которые могут быть в свою очередь Как изменяемые так и неизменяемые Но если углубляться до базовых элементов то окажется что в конечном счёте они также состоят из неизменяемых объектов например список может состоять из других списков которые свою очередь содержат другие списки но в конечном итоге мы доберёмся до списка который будет состоять например из чисел или строк то есть неизменяемых объектов Пусть переменная names ссылается на список с именами состоящий из двух элементов оба из которых относятся к строковому типу строковые элементы неизменяемые об они имеют свой адрес памяти то есть располагают уникальными значениями идентификатора точно так же как переменная name ссылается на список на месте каждого элемента списка находится ссылка на соответствующий ему объект при изменении элемента списка если он относится к неизменяемое на старый объект будет удалена А на его место встанет ссылка на Новый объект на очень познавательном примере разберём как Как работает копирование списков аналогию можно применить и к другим изменяемым объектам Дело было в ресторане семейная пара сделали заказ официант сразу разместил его в базе заказов ресторана данный заказ отражается в системе под именем столика Table One то есть переменная Table One ссылается на список состоящий из ещё одного списка и строки вложенный список соответствует заказу одного человека а строка так как это только вода соответствует заказу второго человека столик номер два заняла ещё одна пара увидев как аппетитно ест гость за столиком номер один мужчина крикнул официанту хочу тоже самое что и у него только в конце трапезы вместо пива буду чай девушка попросила стакан сока услышав хочу то же самое официант Нажал кнопку дублировать заказ после чего в системе создаётся переменная Table 2 для дублирования списка можно использовать несколько способов например прибавить к существующему списку пустой список присвоить новому списку полный срез старого списка воспользоваться функциями лист или копий во всех этих случаях будет создан новый список с новым идентификатором Ну то есть на первый взгляд официант имеет два независимых списка со схожим набором элементов но элементы списков - это ссылки на объекты поэтому когда мы создаём копию списка Таким образом мы создаём Новый объект типа список с новым идентификатором и всеми теми же ссылками на те же самые объекты что и у исходного списка если ссылка приходится на неизменяемый объект то при изменении объекта ссылка просто изменится на Новый неизменяемый объект в нашем случае у нас меняется ссылка С объекта вода на объект сок А если ссылка ведёт на изменяемый объект то тут нужно проявить внимательность Чтобы избежать ошибок в нашем примере официанту нужно изменить пиво на чай если он произведёт замену пива на чай используя список на которой ссылается переменная Table 2 то данные изменения затронуты переменную Table One так как ссылки и из переменной Table One и из переменной Table 2 Ведут к одному и тому же списку таким образом оба клиента получат чай данное копирование называется поверхностным когда создаётся список с новым идентификатором но все его ссылки Ведут к тем же объектам что и у исходного если все элементы списка неизменяемые то проблем при внесении изменений не возникнет Если же в списке имеются изменяемые элементы то лучше использовать глубокое копирование с помощью модуля Copy и функции Deep Copy глубокое копирование создаёт полностью отдельный набор объектов и изменения в копии никак не повлияют на оригинальный список плюсом поверхностного копирования является экономия памяти внутреннее представление чисел с плавающей точкой в Python основано на двоичной системе и следует стандарту I 754 поскольку точность чисел с плавающей точкой ограничена могут возникать небольшие ошибки округления к примеру при вычитании от единицы 99 интерпретатор Python выдаст ответ С небольшой ошибкой также как 1/3 нельзя точно представить в десятичной системе числа десятичной системы с плавающей точкой не имеют точного представления в двоичной системе разберём Когда возникает неточность в данном примере для этого переведём числа в двоичную систему точно так как это делает Python перед вычислением единица при переводе из десятичной в двоичную систему остаётся единицей для перевода 99 воспользуемся следующим алгоритмом умножаем дробную часть на два записываем полученную целую часть на своё место после запятой откидывающаяся к первому шагу Как видно процесс перевода 99 из десятичного представления в двоичное может продолжаться бесконечно в компьютере число будет обрезано до некоторого количества бит что приведёт к незначительной погрешности вычитаем из единицы приближённое число и преобразуем его в десятичный вид Естественно что образованная погрешность сохраняет при перево числа из двоичного представления десятичное если при вычислении требуется высокая точность в Python существует специальный модуль десима который представляет поддержку для работы с десятичными числами с фиксированной точностью в помощи разработчику Python имеет две функции функция DIR возвращает список атрибутов и методов объекта проще говоря с её помощью можно увидеть что доступно для работы с этим объектом так например при передаче данной функции объекта со строковый значением на экран выводятся все методы строки они упорядочены по алфавиту в начале перечисляются методы начинающиеся с двойного подчёркивания это специальные методы также называются волшебными специальные методы определяют что происходит во внутренней реализации при выполнении встроенных операций таких как создание объектов сложения сравнения и так далее например при выполнении какого действия над объектом Python ищет соответствующий специальный метод и вызывает его за волшебными методами идут обычные методы они предназначены для прямого использования в отличие от волшебных методов которые вызываются автоматически при определённых операциях обычные методы нужно вызывать явно функция DIR отлично подходит Когда нужно быстро посмотреть что доступно для работы с объектом без детального описания вторая функция если первая функция показывает название методов и атрибутов то Help раскрывает описание того как с ними работать если в качестве аргумента функции передать один из методов то на экран будет выведено текстовое описание Что делает метод Какие принимает аргументы например при передаче одного из методов списка метода App мы получаем описание что метод получает в качестве аргумента объект и добавляет его в конец списка е вызвать функцию Help Без передачи ей каких-либо аргументов то откроется интерактивный режим где можно ввести название модуля и подробнее о нём узнать например нас интересует модуль Рандом мы видим название модуля ссылка видимо на документацию Так а ну последующая информация не самая точно заточной ити по ссылки так и далее идт описание модуля описание Поэтому если нужно прерваться для выхода из него нужно ввести команду quit Ну в общем вывод чтобы узнать доступные методы используется функция DIR чтобы понять как они работают функция Help в Python имеется специальное значение которое используется Для пустоты или отсутствия данных это значение NAN которое относится к классу npe и является его единственным экземпляром то есть в интерпретаторе Python всегда хранится только одна его копия идентификатор этого значения всегда остаётся одним и тем же Чуть позже это увидим когда используется значение NAN например в программе должна существовать переменная значение которой ещё не определено То есть если у переменной ещё нет конкретного значения оно появится но когда-то позже а переменную уже необходимо инициализировать ей можно присвоить значение N если в функции отсутствует команда Return то по умолчанию она возвращает NAN к примеру результат вызова функции Пак присваивание переменной СН значения True на этом роль функции закончена и она ничего не возвращает при попытке присвоить значение этой функции переменной она будет ссылаться на значение NAN сравним идентификаторы переменных прот и р Несмотря на то что они получены разными путями они равны об этом мы говорили чуть выше что в интерпретаторе хранится только один объект None для сравнения идентификаторов быстрее использовать оператор is он сравнивает идентификаторы двух объектов и в случае если они равны выводит True кстати в логическом контексте NAN интерпретируется как false одна из удобных особенностей функции Python параметры по умолчанию они позволяют задавать значения для аргументов функции которые будут использоваться если при вызове функции их не указать это удобно когда у функции есть стандартное поведение и необходимо оставить возможность его изменять при необходимости для создания значения для параметра по умолчанию при определении функции необходимо после параметра поставить знак равенства и нужно значение параметров со значениями по умолчанию может быть несколько и они должны следовать после обязательных параметров желательно в качестве значений по умолчанию для параметров не использовать изменяемые так как они инициализируется только один раз и могут запомнить изменения между вызовами функции рассмотрим пример определим функцию с двумя параметрами значение первого параметра будет передаваться в качестве аргумента при вызове функции предполагается что это будет набор элементов в виде строки второй параметр по умолчанию пустое множество множество - это изменяемый объект функция должна вывести на экран все уникальные элементы строки в виде мно Мы помним что множество исключает дублирование элементов удобно полученная строка передаётся как набор элементов множеству параметру по умолчанию после чего значение этого параметра выводится на печать при вызове функции первый раз всё отлично при вызове функции второй раз новый набор элементов добавится К объекту множеству который образовался при первом вызове функции добавим функцию вывод на экран значения идентификатора множества Мы видим что при обоих вызовах функции параметрическая переменная ссылается на одно и то же множество так как она имеет один и тот же идентификатор тут необходимо освежить в памяти отличия между определением и вызовом функции для этого обратимся к конспекту про функции на первом этапе необходимо определить функцию с именем например гритинг при вызове которой будет исполняться инструкция вывода сообщения для исполнения функции её необходимо вызвать в написанной программе две инструкции первая создаёт функцию в оперативной памяти вторая инструкция вызывает созданную функцию Что приводит к исполнению её инструкций так вот параметры по умолчанию создаются в момент определения функции то есть на первом этапе когда функция создаётся в оперативной памяти на втором этапе этапе вызова функции параметры по умолчанию считываются из памяти таким образом после первого вы функции Хотя объект остаётся прежним внутренние ссылки его меняются и при втором вызове функции это естественно проявится для того чтобы избежать подобных казусов изменяемым параметрам по умолчанию присваивается значение NAN Затем в теле функции Если значение параметра по умолчанию равно NAN даётся указание создать и присвоить ему экземпляр нужного изменяемого типа в нашей функции появилась строка с описанием работы функции данная строка выделена в тройные кавычки и является строкой документации именно отсюда функция Help получает информацию переданного ей объекта строки документации напомнят разработчику Что делает его код также они предоставляют бесценную информацию всем тем Кому приходится знакомиться с чужим кодом поэтому так важно про них не забывать разберёмся на низком уровне как интерпретатор Python взаимодействует с файлами при вызове функции Open Close при вызове функции Open для открытия файла Python отправляет запрос операционной системе указывая путь к файлу и режим доступа операционная система находит этот файл открывает его и создаёт для него запись в таблице файловых дискрит состоящий из файлового дескриптора и информации о файле подобные таблицы создают для каждого процесса запущенного в операционной системе например таким процессом может быть программа на языке Python при запуске двух программ будет создано две таблицы файловых дескрипторов файловый дескриптор - это уникальный целочисленный номер которому соответствует запись отражающая информацию о файле такую как путь к файлу на диске права доступа режим доступа и так далее таким образом файловый дескриптор - это индекс в таблице файловых дескрипторов по которому впоследствии можно выйти на файл значение файлового дескриптора передаётся программе с его помощью программа сможет обращаться к файлу через операционную систему предъявляя его как ссылку на нужный файл значение файлового дескриптора можно узнать Используя метод filen при открытии нескольких файлов в одной программе они будут иметь разные значения файловых дескрипторов если же программ несколько то операционная система создаёт памяти разные таблицы дескрипторов и может получиться Так что индексы значения файловых дескрипторов из одной таблиц совпадают со значениями и с другой При этом они будут ссылаться на разные файлы после получения файлового дескриптора Python в оперативной памяти создаёт файловый Объект который содержит дескриптор файла тип идентификатор буфер для чтения или записи режим доступа к файлу в зависимости от доступа к файлу разберём работу буферизации при чтении или при записи данных объём буфера ограничен при чтении из файла после открытия файла при первом вызове методов read или readline Python обращается к операционной системе указывая файловый дескриптор и с её помощью загружает объём данных сопоставимый объёму буфера файлового объекта если был вызван метод rline то будет возвращена первая строка из буфера при последующем вызове метода rline будет возвращена следующая строка и так далее пока буфер не опустеет с последующим вызовом метода Redline в буфер вновь загрузится очередная порция данных и будет возвращена первая строка из буфера данный процесс будет продолжаться до тех пор пока файл не будет прочитан полностью если был вызван этот Рид то данные файла будут считываться и возвращаться порциями равными объёму буфера файлового объекта при записи данные получаемые во время работы программы временно накапливаются в буфере файлового объекта данные будут перенесены в файл на диске когда буфер будет заполнен или когда будет вызвана функция Close буферизация ввода и вывода позволяет снизить количество обращений к диску Что повышает эффективность работы с файлами размер буфера зависит от настроек операционной системы Но им также можно управлять вручную для этого у функции Open имеется параметр buffering по умолчанию ей присваивается значение минус единица что следует рассматривать как соответствует настройкам операционной системы Но если изменить значение на единицу то буферизация будет происходить построчно то есть данные будут переноситься в буфер до символа переноса строки таким образом при режиме записи данные из буфера будут записываться в файл на диске всякий раз когда будет появляться символ переноса строки ещё одним способом управления буферизации файлового объекта является использование метода фш данный метод моментально сбрасывает содержимое буфера в файл на диск вне зависимости от настроек параметра бурин данный метод полезен когда убедиться что данные записаны на диск немедленно они задерживаются в буфере и могут быть утеряны а утеряны они могут быть если файл не был закрыт а при этом работа программы завершилась поэтому важно использовать функцию Close при её вызове все данные из буфера будут перенесены в файл и не потеряны также удаляется файловый дескриптор значение счётчика ссылок будет уменьшено И если оно станет равным нулю файловый объект будет Удан и высвободить лучшей альтернативой использования сочетания функции Open Close является конструкция with для открытия файла Необходимо написать инструкцию with Open S File где функция Open та же самая что использовалось при сочетании функции Open Close файл - это имя переменной которой присваивается файловый объект эта строка завершается двоеточием происходит открытие файла и программа переходит от блоку с инструкциями для работы с этим файлом по завершению исполнения инструкций из блока или возникновении ошибки файл будет автоматически закрыт данный способ понижает вероятность утечки данных и делает код более читабельный оба кода являются эквивалентными когда встречает переменную или функцию во время выполнения кода которые они ссылаются но одинаковые имена могут существовать в разных контекстах ссылаясь при этом на разные объекты для управления именами существует система пространств имён которая связывает каждое имя с конкретным объектом и предотвращает конфликты основными типами пространств имён Python являются встроенная содержит функции и объекты встроенные в Python данные объекты доступны в любой части кода глобальная включа име онные на уровне модуля это те имена которые объявлены в основном теле программы вне функций и классов локально относится к именам определённым внутри функций или методов и доступно только в них вложенная используется для функций онх внутри других функций это так называемое обое пространство имн оно доступно вложенным функциям каждое пространство хранит свой набор имн чтобы посмотреть Какие имена доступны во встроенном пространстве имён можно передать функцию DIR функции Print это вернёт список всех встроенных функций исключений и объектов доступных по умолчанию для доступа к глобальному пространству модуля используется функция globals которая возвращает словарь всех имён и объектов модуля естественно в том числе переменных и функций объявленных в данном модуле чтобы посмотреть имена доступные в локальной области внутри функции используется функция locals которая возвращает словар имён в текущем локальном контексте включая те что были определены В текущей локальной области а также доступные имена из вложенного пространства если они были использованы В текущей локальной области в Python поиск имени начинается в зависимости от того Где в коде это имя вызывается если имя вызывается в модуле но вне функций или классов поиск стартует В глобальном пространстве имён соответствующим модулю Затем идёт встроенное пространство имён Если же переменная или функция вызывается внутри другой функции или класса то поиск начинается с локального пространства затем продолжается во вложенном далее глобальном и наконец во встроенном пространстве имён То есть поиск всегда идёт вверх по иерархии пространств имён но никак не вниз при этом переход на следующее пространство происходит только в том случае если в текущем пространстве не удалось найти значение имени встроенное пространство имн для всей программы едино глобальных пространств имён может быть несколько Например если в программе используется более одного модуля дополнительно у каждого модуля может быть своя вариация вложенности функций что ведёт к образованию различного множества локальных пространств имён внутри каждого модуля как программе не запутаться в именах в такой многоуровневой структуре у каждого пространства имени имеется область видимости область видимости определяет Где в коде можно получить доступ к его именам встроенные имена доступны везде глобальные имена видимы в модуле где они определены имена определённые в оборачивают для всех её вложенных функций локальные имена видны только в функции где они были определены модуль в Python - это файл содержащий Python код модули помогают разбивать код на логические независимые части и упрощают работу над проектами модуль может содержать функции классы переменные и исполняемые инструкции которые можно использовать в других программах использование модулей позволяет переиспользовать код и упрощает его поддержку для создания модуля достаточно сохранить файл в котором определены необходимые функции переменные и классы с расширением па например cle то для использования объектов из модуля в другом файле Имеется три способа первый при необходимости использования лишь некоторых атрибутов из модуля можно воспользоваться механизмом импортирование в стиле From через запитую можно указать несколько атрибутов для импортирование данная команда пополнит глобальное пространство имён указанными атрибутами в этом можно убедиться вызвав функцию globals импортируемый таким имена в дальнейшем можно использовать точно так же как если бы они были определены в текущем модуле то есть для их вызова достаточно написать их имя если вместо имени атрибутов использовать звёздочку то глобальное пространство имён текущего модуля пополнится всеми общедоступными именами из указанного модуля данный подход имеет свои риски Например если таким образом импортированы атрибуты из разных модулей но с одинаковыми именами в текущем пространстве имён значение которое было импортирование для импортирование модуля целиком предпочтительнее использовать команду импорт в таком случае в глобальном пространстве имён будет создана переменная ссылающихся кстати имя переменной не Обязательно должно совпадать с именем модуля его можно изменить используя команду S для извлечения значения одного из атрибутов объекта на который ссылается переменная нужно указать имя этой переменной и через точку имя атрибута модуль определяет группу взаимосвязанных функций Python или других объектов и упрощает работу мелких фрагментов кода по мере роста проекта использовать один огромный модуль не чно имеет смысл разбить его на несколько логически связанных модулей и объединить их в каталог с программным кодом называемый пакетом по аналогии с тем как модули группируют взаимосвязанные функции пакеты группируют взаимосвязанные модули имя модуля определяется именем файла имя пакета определяется именем главного каталога пакета пакет в Python - это каталог который содержит несколько модулей и возможно вложенный подкаталоги с моду он служит для логического объединения модулей связанных по функциональности например пакет для работы с геометрией может содержать модули скол squ Triangle каждый из которых включает функции и классы для работы с соответствующими фигурами чтобы пакет распознавал как пакет в его главном каталоге обычно создаётся файл init Этот файл может быть пустым если он служит только для указания Python что каталог - это пакет Однако в него Также можно добавить код чтобы выполнить какую-то инициализацию при первом импорте пакета или импортировать общие функции и переменные которые будут доступны при обращении к пакету как к целому после создания пакета можно импортировать его модули используя точечную нотацию которая указывает путь к модулю Например если у нас пакет геометрии с под модулями то мы можем импортировать функции следующим образом если функции перечис пакета то можно импортировать их напрямую например добавив команду импорта функции мы позволяем импортировать её напрямую из геометрии в ИТ можно указать Какие модули доступны при импорте пакета геометрии как целого ещё более крупная структура чем пакеты - это библиотеки библиотека - это сборник модулей и пакетов Объединенных для решения определенных задач или поддержки конт приложений в отличие от модуля который может содержать только одну функцию или класс и пакета состоящего из нескольких модулей библиотека представляет собой более крупный набор инструментов она может включать десятки и даже сотни модулей и пакетов организованных для удобного использования Python включает обширную стандартную библиотеку которая поставляется вместе с самим Python она содержит готовые модули и пакеты для выполнения различных задач рабо с файлами сетью математическими вычислениями временем и датами созданием интерфейсов и многого другого библиотеки импортируются также как и модули и пакеты с помощью инструкций импорт